-- Optional local/demo seed.
-- The real per-workspace defaults are created by public.seed_default_workspace_data()
-- when public.create_initial_workspace() is called after authentication.

-- This file intentionally avoids hardcoded users and sensitive data.
